# 问题与变更记录

## 2026-07-30：发布最新代码并清理仓库边界

### 现有逻辑

- `high_alloy_incl` 模板在注册表、模板元数据和前端默认文案中统一显示为“探索模式”。
- 项目通过 FastAPI、单 worker 任务队列和 FactSage/mock 运行链路完成温度扫描、结果解析与导出。
- `readme.md` 与 `AGENTS.md` 记录当前架构、运行方式、约束和验证要求。

### 问题原因

- 本地未推送提交曾混入 `mem/` 会话文件；这些文件属于本地 Agent 记忆，不是项目源码。
- `output/` 保存软著文档、截图、运行日志和临时数据库等生成物，不应随源码发布。
- 全局 `*.res` 忽略规则误伤了 mock 和测试依赖的 `demo/Equi2.res`、`demo/Equ2222i.res`，导致干净检出后测试缺少输入文件。

### 解决办法

- 在 `.gitignore` 中明确忽略 `mem/` 和 `output/`。
- GitHub 发布提交只包含项目代码、项目文档和前端资源，不包含本地会话或运行生成物。
- 为两个固定 demo `.res` 增加例外并纳入版本控制，其他计算生成的 `.res` 继续忽略。
- 模板名称在后端注册表、模板元数据和前端回退文案中保持一致，避免界面显示不统一。

### 防止重复犯错

- 提交前执行 `git status --short`，确认没有 `mem/`、`output/`、数据库、日志或运行工作目录进入暂存区。
- 禁止使用 `git add .`；只暂存本次确认过的项目文件。
- 推送前检查 `git diff --cached --stat` 和待推送提交列表。
